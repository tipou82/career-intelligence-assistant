feedparser>=6.0.11
PyYAML>=6.0.1
pytest>=8.0.0

# LLM classifiers (optional — install the one you want to use)
openai>=1.30.0          # pip install openai
# anthropic>=0.25.0     # pip install anthropic

# HTML report rendering (stdlib html module is used — no extra deps needed)

# Email digest (uses stdlib smtplib — no extra deps needed)

# Job market signals (uses stdlib urllib — no extra deps needed)

# Press release scraping (feedparser handles RSS/Atom — no extra deps needed)
